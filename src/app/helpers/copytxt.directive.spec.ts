import { CopytxtDirective } from './copytxt.directive';

describe('CopytxtDirective', () => {
  it('should create an instance', () => {
    const directive = new CopytxtDirective();
    expect(directive).toBeTruthy();
  });
});
