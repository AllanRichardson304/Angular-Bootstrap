import { PopupconfirmDirective } from './popupconfirm.directive';

describe('PopupconfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupconfirmDirective();
    expect(directive).toBeTruthy();
  });
});
